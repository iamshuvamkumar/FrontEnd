var amt = parseInt(prompt("Enter Amount"))

if(amt>2000){
    var age = Number(prompt("Enter your age"))
    if(age>18){
        document.write("you can go")
    }
    else{
        document.write("noo you can't go your age is undar 18")
    }
}
    else{
        document.write("noo you can't go your amount is low")
    }

